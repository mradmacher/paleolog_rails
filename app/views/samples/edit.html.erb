<%= render_title "#{@sample.well.region.name}" %>
<%= render 'navigation' %>
<%= render_actions [['Cancel', sample_path( @sample )]] %>
<%= render_heading 'Editing sample' %>

<%= render :partial => 'form' %>
