<%= form_for(@research_participation) do |f| %>
  <%= f.label :user_id %>
  <%= f.collection_select :user_id, @other_users, :id, :name %>
  <%= f.hidden_field :project_id %>
  <%= f.submit 'Add' %>
<% end %>
