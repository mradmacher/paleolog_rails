<%= render_title 'Projects' %>

<% if current_user.admin? %>
	<%= render_actions [['Add project', new_project_path]] %>
<% end %>
<%= render_heading 'Projects' %>

<ul>
	<% @projects.each do |project| %>
		<li><%= link_to project.name, project %></li>
	<% end %>
</ul>

